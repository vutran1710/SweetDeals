- type: serial
  steps:
    # - name: local_testin
    #   exclude: master
    #   service: sweetdeals
    #   command: npm run verify
    - name: publish_new_image
      tag: master
      service: sweetdeals
      command: npm run docker:publish
    - name: deploy with kubec
      service: kubectl
      # command: chmod +x shellscript.sh && . ./shellscript.sh get_version
      command: ls -al
